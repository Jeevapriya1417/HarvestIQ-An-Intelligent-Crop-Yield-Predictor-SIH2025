Always generate code that is non‑redundant, consistent with existing style, avoids repeating past mistakes, modifies only what is necessary, and prioritizes clarity and maintainability over unnecessary complexity.